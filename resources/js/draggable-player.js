export function draggablePlayer() {
    const playerDragHandler = document.querySelector("#playerDragHandler");

    // playerDragHandler.addEventListener("drag", (e) => {
    //     console.log("darged");
    // })
}
